task speak {
	doLast {
		println 'rootProjectSpeak'
	}
}

configure(subprojects.findAll {it.name == 'subproject'}) {
    task speak {
        doLast {
            println 'subprojectSpeak'
        }
    }
}
